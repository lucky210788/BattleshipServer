export interface User {
  ships: Array<Object>,
  token: string,
  yourTurn: boolean
}

export interface CoordinatesSips {
  ships: Array<Object>,
  token: string
}

export interface Token {
  token: string
}
